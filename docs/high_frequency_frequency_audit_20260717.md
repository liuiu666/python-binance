# 2026-07-17 高频策略频率与前向失败审计

## 结论

V9/V13 不能上线，当前状态应为 `REJECTED_FORWARD_FAIL`。

原因不是“今天单太少”这么简单，而是今天已经给出了新的前向失败证据：

- 2026-07-17 上海日内 shadow 原始结果：`0/3`，PnL `-15U`。
- 去掉 V9/V13 同秒同方向重复信号后，独立机会结果：`0/2`，PnL `-10U`。
- V9 与 V13 在 `2026-07-17T08:49:59Z` 同时给出 DOWN，开仓价和结算价一致，说明它们不是独立风险来源。
- 本地 `data/server_latest` 的秒级成交和订单簿文件没有覆盖今天完整实时窗口，不能写成“本地原始高频回放已通过今天”。

因此，之前“V13 是当前更好候选”的说法已经失效。历史胜率只能说明它曾经值得观察，不能抵消今天前向 0%。

## 今天亏损明细

| 时间(上海) | 策略 | 方向 | 结果 | 开仓价 | 结算价 | PnL |
| --- | --- | --- | --- | ---: | ---: | ---: |
| 2026-07-17 08:23:28 | V9 | UP | lost | 63829.13 | 63696.00 | -5U |
| 2026-07-17 16:50:08 | V9 | DOWN | lost | 62855.02 | 62892.04 | -5U |
| 2026-07-17 16:50:08 | V13 | DOWN | lost | 62855.02 | 62892.04 | -5U |

第二、第三笔是同一交易想法重复记录。以后统计频率和胜率时，必须同时看原始单数和去重后的独立机会数。

## 为什么不能继续用历史报告背书

旧历史候选报告显示：

- 66 单
- 胜率 `72.73%`
- PnL `+102U`
- 最大回撤 `11U`
- 最大连亏 `2`
- 日均约 `6` 单

这些数字现在只能作为“历史候选来源”，不能作为上线理由。严格规则是：历史好看但前向当天 `0%`，直接进入否决/复核，而不是继续调阈值把它救回来。

## 为什么今天单少

今天策略多数时间没有信号，主要是门槛没有放行，而不是采集断了。

最新线上健康接口显示：

- 线上 1m、多空比、持仓、秒级成交、订单簿都在更新。
- 当前 V9/V13 都在 shadow 扫描。
- 当前信号为 `null`，原因是 `liq_normal_not_ready`。
- 快照中主要未达标项是 10 分钟波动低于策略下限，例如 `3.35bp`，要求 `5.8-55bp`。

这说明当前环境不符合 V9/V13 试图捕捉的“短周期正态震荡假突破回收”。少单本身不是 bug，但今天放出来的单质量失败，所以不能用“门槛严格”来解释掉亏损。

## 本地数据覆盖限制

本地文件覆盖情况：

- `btcusdt_1m.csv`：最新到上海 `2026-07-17 16:33:00`。
- `btcusdt_1s_trades.csv`：最新到上海 `2026-07-16 23:21:36`。
- `btcusdt_orderbook_1s.csv`：最新到上海 `2026-07-16 23:24:31`。

线上接口显示服务器自身秒级成交和订单簿已经更新到上海 `2026-07-17 18:24` 附近，但本地副本没有同步到这个时间。因此今天只能确认“线上前向 shadow 统计失败”，不能宣称“本地完整原始高频回放已经验证完美”。

要做真正的本地今天回放，需要先拿到服务器 `/opt/btc-binary-options/data` 下今天的秒级成交与订单簿文件，或新增只读导出接口。

## 后续标准

从现在开始，策略候选必须满足这些门槛：

- 不用历史胜率单独决定上线。
- 任何候选实盘前至少需要 20 笔新的前向 shadow 独立机会。
- 前向胜率至少 `63%`。
- 前向最大连亏不超过 `2`。
- 同族策略同秒同方向信号必须去重，不能把重复信号当频率。
- 本地秒级成交和订单簿必须覆盖验证日期，否则报告只能写“线上前向统计”，不能写“本地完整回放”。

配套脚本：`py/audit_forward_strategy_rejection.py`。

最新生成报告：

- `tmp/forward_strategy_rejection_20260717.json`
- `docs/forward_strategy_rejection_20260717.md`
